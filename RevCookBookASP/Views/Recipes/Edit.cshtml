@model RevCookBookASP.ViewModels.RecipeViewModel

@{
    ViewData["Title"] = "Edit";
    ViewData["FormSubmitButtonValue"] = "Save";
}

<h2>Edit</h2>

<h4>Recipe</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @Html.Partial("Recipes/_CreateEditForm")
    </div>
</div>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
