@model RevCookBookASP.ViewModels.RecipeViewModel

@{
    ViewData["Title"] = "Create";
    ViewData["FormSubmitButtonValue"] = "Create";
}

<h2>Create</h2>

<h4>@Html.DisplayNameFor(model => model)</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @Html.Partial("Recipes/_CreateEditForm")
    </div>
</div>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
