@model RevCookBookASP.ViewModels.IngredientViewModel

@{
    ViewData["Title"] = "Edit";
    ViewData["FormSubmitButtonValue"] = "Save";
}

<h2>Edit</h2>

<h4>Ingredient</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @Html.Partial("Ingredients/_CreateEditForm")
    </div>
</div>

<div>
    @Html.ActionLink("Back to list", "Index")
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
